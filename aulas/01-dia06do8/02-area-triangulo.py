# 2. Peça ao usuário para inserir a base e a altura de um triângulo e calcule a área.

base = float(input("Insira a medida da base do triangulo: "))
height = float(input("Insira a altura do triangulo: "))

area = (base * height) / 2

print(f"Área do triangulo: {area}")